# Student.java

## 📘 Overview
This project contains a simple Java program demonstrating the concept of **classes and objects** in Java.  
The `Student` class encapsulates details about a student such as name, roll number, and marks, and includes methods to display and manage this data.

## 🧠 Concepts Used
- Object-Oriented Programming (OOP)
- Class and Object
- Encapsulation
- Getter and Setter methods
- Constructors

## 🧩 File Description
- **Student.java** — Contains the definition of the `Student` class with fields, constructors, and methods to handle student details.

## 🚀 How to Run
1. Make sure you have **Java JDK** installed on your system.
2. Open a terminal or command prompt.
3. Compile the Java file:
   ```bash
   javac Student.java
